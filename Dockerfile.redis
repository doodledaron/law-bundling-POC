FROM redis:7-alpine

# Copy custom Redis configuration if needed
# COPY redis.conf /usr/local/etc/redis/redis.conf

EXPOSE 6379

# Use the default command: redis-server
CMD ["redis-server", "--appendonly", "yes"]