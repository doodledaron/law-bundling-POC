# MINIMAL SECURITY UPDATES - Only fixes critical vulnerabilities with minimal version changes
# This is the safest approach - only updates packages with known exploits

# Core Python packages (unchanged)
click==8.1.8
colorama==0.4.6
decorator==5.1.1
setuptools==75.8.0
six==1.17.0
packaging==24.2
typing_extensions==4.12.2

# PPStructure - match working version exactly (unchanged)
paddleocr==3.0.0

# FastAPI and web framework - MINIMAL SECURITY UPDATES
fastapi==0.109.1  # Minimal update from 0.103.1 - fixes CVE-2024-24762 (ReDoS) only
uvicorn==0.23.2  # Keep same - compatible with FastAPI 0.109.1
python-multipart==0.0.22  # Fixes ReDoS, DoS boundary, and arbitrary file write CVEs
Jinja2==3.1.6  # CRITICAL: fixes CVE-2024-56201, CVE-2025-27516 (code execution)
starlette==0.35.0  # Minimal update from 0.27.0 - fixes major DoS issues

# Image processing - MINIMAL SECURITY UPDATES
opencv-python-headless<=4.6.0.66  # Keep same - working version
pillow==12.1.1  # Fixes all Pillow CVEs through CVE-2026-25990 (code execution, buffer overflow)

# OCR and document processing - MINIMAL SECURITY UPDATES
pdf2image==1.16.3  # Keep same - working version
pypdf==3.1.0  # Minimal migration from PyPDF2 3.0.1 - fixes CVE-2023-36807, maintains API compatibility
pymupdf==1.23.14  # Keep same - working version
numpy>=1.24.3

# Scientific computing - compatible with numpy 1.24.3 (unchanged)
scipy>=1.11.0,<1.12.0
scikit-learn>=1.3.0,<1.4.0

# Utilities and parsing - MINIMAL SECURITY UPDATES
lxml==5.3.1  # Keep same
beautifulsoup4==4.13.3  # Keep same
requests==2.32.4  # Fixes CVE-2024-47081 (.netrc credential leak) and verify=False bypass
urllib3==2.2.3  # Keep same for now - vulnerabilities are DoS only, not RCE
tqdm==4.67.1
python-dateutil==2.9.0.post0
python-docx==1.1.2

# Task queue and background processing (unchanged)
celery==5.3.4
flower==2.0.1
redis==4.6.0

# AI/ML integration (unchanged)
google-genai==0.7.0
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
aiofiles==23.2.1
pydantic==2.5.0

# Additional utilities (unchanged)
python-magic==0.4.27
psutil==5.9.6

# Excel support (unchanged)
pandas==1.5.3
openpyxl==3.1.2

# Testing (unchanged)
pytest==7.4.3
pytest-asyncio==0.21.1
